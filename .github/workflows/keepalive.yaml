name: Keepalive

on:
  workflow_dispatch:
  schedule:
    - cron: "*/5 * * * *"

jobs:
  keepalive:
    runs-on: ubuntu-latest

    steps:
      - name: Send keepalive request
        run: curl https://garbage-classification-web.onrender.com/